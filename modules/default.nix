# NixOS modules. Currently empty — a reusable dsh systemd module will land
# here once the service module is parameterized (it currently lives in the
# private machine flake). Keep this attr for NUR-layout compatibility.
{
}
